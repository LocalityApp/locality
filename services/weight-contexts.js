
// get order of contexts from user input
// get ranked neighborhood codes by context
// do math to return top 3 neighborhoods by weight

// multiply neighborhood score by context rank
// sum and sort neighborhood scores across context
// send top neighborhood codes to front for polygon mapping

//
module.exports = go = (msg) => {
    console.log(msg);
};